// Response shape for
// POST /campaigns/{campaignId}/character-relationships/{membershipCharacterRelationshipId}/revoke
// (character-relationship-management checkpoint).
export interface RevokeCharacterRelationshipResponse {
    membership_character_relationship_id: string
}
