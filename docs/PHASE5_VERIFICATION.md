# Phase 5 Verification Checklist

Records the verification performed for Phase 5 (Locations and dungeon play) per [PLAN.md §23](PLAN.md#23-delivery-phases) and the exit-review process in [§23.1](PLAN.md#231-phase-exit-review). Phase 5 also pulled a minimal slice of the knowledge domain forward from Phase 7 to satisfy its own exit criteria — see "Preceded by a scope decision" below and [DATABASE_MODEL.md §26](architecture/DATABASE_MODEL.md#26-reconciliation-notes-phase-5) for the full account. This document covers the original build (revisions 038–042) and four corrections passes: two pre-merge (043–047 and 048–051) and two post-merge (052–055 and 056). The first post-merge pass resolved the original five-item register, but a fourth review found one opposing dungeon-area write path still lacked shared locking and the existing concurrency tests did not prove that their original waiting statements resume and revalidate. The second post-merge pass (revision 056) closed both gaps — see [Fourth exit review corrections](#fourth-exit-review-corrections-2026-08-03) below. [PHASE5_REMAINING_ISSUES.md](PHASE5_REMAINING_ISSUES.md) is now a closed historical record, and Phase 5 is complete.

## Exit Criteria

- [x] A party can enter and navigate a multi-room dungeon — `campaign.character_location_history` records arrival/departure per timeline under the full ADR 0010 interval contract (revision 043, `location_period` made `NOT NULL` by revision 050 and genuinely backfilled for any pre-existing rows by revision 052), with the open (`departed_at_world_time_id IS NULL`) row as current location. `tests/scenario/test_dungeon_navigation.py::test_a_party_can_enter_and_navigate_a_multi_room_dungeon` proves this at the level the exit criterion actually describes: a campaign, a party, a party membership, and a three-room dungeon, with the character moving room to room and the resulting history and current location verified — not a bare character with no party involved.
- [x] Hidden connections remain distinct from party knowledge — `world.area_connections.is_hidden` (and the equivalent column on features/hazards/interactables) is a structural fact with no party reference anywhere in its table; discovery is recorded separately in `knowledge.party_discoveries`, scoped per party. `test_knowledge_domain.py::test_a_hidden_connections_own_row_never_reveals_party_knowledge` asserts both structurally (`world.area_connections` has no `discover`/`known`-named column) and behaviorally (two parties on the same timeline have independent discovery records for the same connection, which itself stays `is_hidden = true` throughout); the navigation scenario above checks the same thing in context.
- [x] Actions can alter dungeon state — all five typed state tables (`campaign.location_state`, `.area_connection_state`, `.area_feature_state`, `.hazard_state`, `.interactable_state`) support in-place transitions, exercised by `test_dungeon_timeline_state.py` (e.g. a connection moving from locked to open, a hazard from armed to disarmed) and correctly maintain `updated_at` on every change (revision 046), proven by mutating a real state column across genuinely separate transactions rather than only by overriding the column directly (`test_dungeon_state_updated_at.py`).

The listed criteria and tests were verified against the deployed AWS `dev` RDS instance, per [§23.0](PLAN.md#230-aws-verification-policy): upgrade to `head` through all 19 revisions (038–056), downgrade/upgrade round trips covering all four corrections passes (including the revision-052 splice point in both directions), `alembic check` clean, and the full test suite — see "Verification commands and results" under the fourth corrections pass below. Deep/corrupt-hierarchy, populated-upgrade, complete-whitespace, and now child-location-lock coverage are all included, and the concurrency suite proves resumed-waiter behavior rather than only lock-timeout attachment.

## Preceded by a scope decision

PLAN.md's Phase 5 deliverable list names "discovery records" and its exit criteria require hidden connections to stay distinct from party knowledge, but the documented shape of discovery (`knowledge.party_discoveries`, DATABASE_MODEL.md §15) is tied to `knowledge.knowledge_items` — a table DATABASE_MODEL.md's own implementation order (§23) places in Phase 7, after locations, interactions/events, and quests. This was surfaced and discussed with the user before writing schema (matching the process CLAUDE.md §7 asks for) rather than resolved unilaterally. The decision: pull the minimal slice of the knowledge domain forward — `knowledge.knowledge_items`, `knowledge.entity_knowledge`, `knowledge.party_discoveries`, plus the two lookups they need — using their documented Phase 7 shape, rather than invent a smaller Phase-5-only table Phase 7 would need to reconcile later. `knowledge.knowledge_versions`, `.information_transfers`, `.expertise_domains`/`.character_expertise`, and `.public_knowledge` remain genuinely deferred to Phase 7. See DATABASE_MODEL.md §26 for the full reconciliation note, including the simplified single-subject shape used on `knowledge.knowledge_items` in place of the plural "subject entities" DOMAIN_MODEL.md §15.1 describes.

## What Was Built

Nineteen revisions, 24 new tables, and six columns added to previously existing tables (one by revision 042, two by revision 043, two by revision 047, and one by revision 048; revisions 050, 051, and 055 tighten/constrain existing columns without adding new ones; revisions 049, 052, 053, 054, and 056 add no columns at all).

| Revision | Delivers |
|---|---|
| `038_locations` | `world.locations` (class-table-inheritance root, self-referencing `parent_location_id` with a same-world trigger), `world.settlements` (`population`), `world.buildings` (`building_use`); nine `core.entity_types` rows (`location` plus six no-subtype-table leaves — plane, continent, nation, region, district, geographic_feature — plus `settlement`, `building`) |
| `039_dungeon_structures` | `world.dungeons` (`danger_level`), `world.dungeon_areas` (with a trigger requiring a dungeon-typed parent), `world.connection_types` (seeded lookup), `world.area_connections` (same-world but deliberately not same-dungeon enforced — teleportation links may cross dungeons), `world.area_features`, `world.area_hazards`, `world.area_interactables` (none entity-rooted; each carries `is_hidden`) |
| `040_dungeon_timeline_state` | `campaign.location_state`, `.connection_statuses` (seeded), `.area_connection_state`, `.area_feature_state`, `.hazard_statuses` (seeded), `.hazard_state`, `.interactable_statuses` (seeded), `.interactable_state` — five trigger functions, one per table, each reaching `world_id` through a different join path |
| `041_knowledge_domain` | `knowledge.knowledge_types` (seeded), `.truth_statuses` (seeded), `.knowledge_items` (entity-rooted, at-most-one-subject CHECK across five typed subject columns), `.entity_knowledge` (one current belief per timeline/item/knower), `.party_discoveries` (exactly-one-recipient CHECK, partial unique indexes per recipient kind) |
| `042_close_phase4_location_refs` | Closes Phase 4's two forward references: `character.characters.origin_location_id` (same-world trigger) and `campaign.character_location_history` (one open/current row per timeline+character via partial unique index — superseded by revision 043 below) |
| `043_character_location_temporal` | Upgrades `campaign.character_location_history` to the full ADR 0010 interval contract: `arrived_at_world_time_id` required, derived `location_period INT8RANGE`, `EXCLUDE USING gist (timeline_id, character_id, location_period WITH &&)`, both world-time endpoints' `ON DELETE` changed from `SET NULL` to `RESTRICT` |
| `044_dungeon_mutation_safety` | Makes `world.area_connections`' endpoints and `world.area_features/area_hazards/area_interactables.dungeon_area_id` immutable (reuses `core.enforce_immutable_columns()` from revision 030); adds a containment-cycle guard and a dungeon-parent revalidation trigger to `world.locations` |
| `045_knowledge_timestamp_world` | Extends `knowledge.enforce_entity_knowledge_world()` and `knowledge.enforce_party_discovery_world()` to also validate `learned_at_world_time_id`/`discovered_at_world_time_id` |
| `046_dungeon_state_updated_at` | Attaches `core.set_updated_at()` to all five revision-040 dungeon-state tables |
| `047_realm_conditional_routes` | Registers the missing `realm` location kind; adds `world.area_connections.is_conditional`/`condition_description` |
| `048_entity_type_change_protect` | Adds `core.entity_types.required_subtype_pk_column` (paired explicitly with `required_subtype_table`); `core.enforce_entity_type_change()`, a generic `BEFORE UPDATE OF entity_type_id` trigger on `core.entities` protecting every registered subtype from being stranded by a parent-side type change; `world.enforce_dungeon_type_change_preserves_areas()`, a dungeon-specific second trigger closing the case where `world.dungeons` was already deleted but `world.dungeon_areas` children remain |
| `049_location_containment_lock` | `CREATE OR REPLACE` on revision 044's `world.enforce_location_no_cycle()`: acquires a per-world `pg_advisory_xact_lock` (sorted across both worlds involved) before reading the containment ancestry, closing the tested two-writer race. (Completed by revision 054 below — the recursive walk's depth cutoff still truncated silently rather than detecting a repeated node or raising on cutoff until then.) |
| `050_char_location_period_notnull` | `campaign.character_location_history.location_period` made `NOT NULL`, matching `campaign.party_memberships.effective_period`; corrects the column and table `COMMENT`s. (Depends on revision 052, spliced immediately before it — see that row below — for the actual backfill; this revision only asserts zero remaining NULLs.) |
| `051_conditional_route_semantics` | `ck_area_connections_conditional_description_paired` on `world.area_connections`: a conditional route requires a non-null description containing something other than ordinary spaces; an unconditional route must have no description. (Completed by revision 055 below — tabs/newlines/carriage-returns were not trimmed until then.) |
| `052_char_location_backfill` | **Spliced between 049 and 050** (050's `down_revision` repointed here — a narrowly-scoped, explicitly recorded exception to the forward-only migration policy; see the revision's own docstring). Backfills `location_period` for any `campaign.character_location_history` row that predates revision 043's derivation trigger by re-firing that trigger via a no-op `UPDATE`, reusing its validation and derivation logic unchanged rather than duplicating it. |
| `053_entity_subtype_change_lock` | `CREATE OR REPLACE` on five functions (`core.enforce_entity_subtype()`, `core.enforce_entity_type_change()`, `world.enforce_dungeon_type_change_preserves_areas()`, `world.enforce_dungeon_area_parent_dungeon()`, `world.enforce_dungeon_area_parent_dungeon_on_update()`): adds per-entity `pg_advisory_xact_lock` protection for generic subtype-vs-retype and proposed-parent-dungeon-vs-retype conflicts. (Completed by revision 056 below — it did not lock the child location shared by `world.dungeon_areas` insertion and direct `world.locations.parent_location_id` mutation until then.) |
| `054_location_cycle_detection` | `CREATE OR REPLACE` on revision 044/049's `world.enforce_location_no_cycle()`: adopts PostgreSQL's native `CYCLE ... SET ... USING path` clause for real repeated-node detection (not a depth heuristic), and now raises a clear error if the depth safety bound is reached instead of silently treating an incomplete walk as proof of anything. |
| `055_conditional_route_whitespace` | Replaces `ck_area_connections_conditional_description_paired`'s blank-detection with `condition_description ~ '\S'` (any non-whitespace character), rejecting tab-only, newline-only, carriage-return-only, and mixed-whitespace-only descriptions the same way a space-only one was already rejected. |
| `056_dungeon_area_child_lock` | `CREATE OR REPLACE` on `world.enforce_dungeon_area_parent_dungeon()` and `world.enforce_dungeon_area_parent_dungeon_on_update()`: each now acquires a `pg_advisory_xact_lock` keyed on the child location, first, before reading either the child's parent or whether it already has a `world.dungeon_areas` row — closing the write skew between inserting a dungeon area and directly changing that same child's `parent_location_id`. |

## Recurring Obligations ([§23.1](PLAN.md#231-phase-exit-review))

| Obligation | Result |
|---|---|
| Object ownership | All 24 new tables owned by `migration_owner` (schema-level default privileges from revision 001 apply automatically); revisions 043–056 alter existing tables and add functions/triggers/columns/constraints, no new tables |
| Default privileges | `app_read_write`/`app_read_only` asserted per-table in `test_role_grants.py`, covering all 24 tables |
| Seed idempotency | `world.connection_types` (9 rows), `campaign.connection_statuses`/`.hazard_statuses`/`.interactable_statuses` (5/5/6 rows), `knowledge.knowledge_types`/`.truth_statuses` (7/6 rows) all seeded with `ON CONFLICT (code) DO NOTHING`, same pattern as every prior lookup; revision 047 adds one `core.entity_types` row (`realm`) the same way. Revision 048's backfill of `required_subtype_pk_column` and revision 052's backfill of `location_period` are both plain conditional `UPDATE`s, not `INSERT ... ON CONFLICT` — naturally idempotent regardless (re-running either matches zero or the same rows), verified as part of the bounded downgrade/upgrade round trips below rather than a separate idempotency test, since neither is a seed *row* that could duplicate. |
| Constraint tests | 1,082 tests total (13 unit, 1,068 database, 1 scenario), up from 1,080 (13/1,066/1) at the third corrections pass's exit. New this (fourth) round: two two-connection tests in `test_entity_type_change_protection.py` covering both starting orders of the child-lock race (`test_a_concurrent_dungeon_area_insert_and_child_parent_clear_is_serialized`, `test_a_concurrent_child_parent_clear_and_dungeon_area_insert_is_serialized`). Rewritten this round, same file: all three existing revision-053 concurrency tests, from a `lock_timeout`-and-third-connection-retry pattern to a genuine background-thread-plus-`pg_stat_activity`-poll pattern proving the original waiting statement resumes and revalidates, satisfying the previously-open resumed-waiter requirement. Third-round additions (`test_phase5_populated_upgrade.py`, and the extensions to `test_dungeon_structural_mutation_safety.py`/`test_dungeon_structures.py`) are unchanged this round. |
| Comments and FK indexes | Zero tables without a comment (`test_schema_documentation.py`); every foreign key indexed, including partial indexes for nullable FKs (`test_every_foreign_key_is_indexed`) |
| Downgrade | Bounded round trip verified directly against `dev` for five revision ranges: `downgrade 037_character_language_ruleset` → `upgrade head` (original build), `downgrade 042_close_phase4_location_refs` → `upgrade head` (first corrections pass, re-run twice more after two in-flight fixes to revision 043), `downgrade 047_realm_conditional_routes` → `upgrade head` (second corrections pass, re-run once more after adding the dungeon-specific trigger to revision 048), `downgrade 047_realm_conditional_routes` → `upgrade head` again (third corrections pass — this range now also exercises the revision-052 splice point in both directions, since 052 sits between 049 and 050), and `downgrade 055_conditional_route_whitespace` → `upgrade head` (fourth corrections pass). The full downgrade-to-`base`/upgrade-to-`head` round trip was not run locally in any pass — doing so would wipe every prior phase's data on the shared `dev` instance — but GitHub Actions run [`30780139624`](https://github.com/NemesisGhost/dnd_ai/actions/runs/30780139624) ran it for the original build ("Test downgrade (full round trip to base)"), and a from-scratch `alembic upgrade head` against a fresh, empty database was run directly (not just via the test suite) to confirm the revision-052 splice is correctly traversed on a true fresh install, not only on `dev`'s already-past-that-point state; each pytest run is itself a further from-`base` migration proof, since `tests/conftest.py` provisions and migrates a fresh ephemeral database to `head` for every session. |
| CI green | The original build was confirmed by GitHub Actions run [`30780139624`](https://github.com/NemesisGhost/dnd_ai/actions/runs/30780139624) on [PR #5](https://github.com/NemesisGhost/dnd_ai/pull/5), later merged to `main`. Each corrections pass used a fresh run: the first by [`30785396702`](https://github.com/NemesisGhost/dnd_ai/actions/runs/30785396702), the second by [`30801159031`](https://github.com/NemesisGhost/dnd_ai/actions/runs/30801159031)/[`30803444653`](https://github.com/NemesisGhost/dnd_ai/actions/runs/30803444653), the third by [`30835071145`](https://github.com/NemesisGhost/dnd_ai/actions/runs/30835071145) on PR #6, and the fourth (revision 056) by the run recorded under "Fourth exit review corrections" below. All passed. |

## Bugs and Gaps Found

1. **A trigger function's error message masked a CHECK constraint.** `knowledge.enforce_party_discovery_world()` originally looked up a recipient's world unconditionally; when both `party_id` and `knower_entity_id` were `NULL` (violating `ck_party_discoveries_exactly_one_recipient`), the `BEFORE INSERT` trigger ran first and raised its own "mixes worlds" error before the `CHECK` constraint got a chance, since PostgreSQL evaluates `BEFORE ROW` triggers before per-row constraints. Fixed by having the trigger return early when both recipient columns are `NULL`, letting the `CHECK` constraint report the more specific error. Found by `test_a_discovery_needs_exactly_one_recipient`.
2. **Two `TEXT` column defaults needed an explicit cast in `tables.py`.** `knowledge.knowledge_items.sensitivity` and `knowledge.entity_knowledge.awareness_level` both declare a string `server_default`; PostgreSQL normalizes a literal-string default on a `TEXT` column to include `::text` in `information_schema.columns.column_default`, so `tables.py` had to declare `text("'public'::text")` / `text("'aware'::text")` rather than the bare literal to match, caught by `test_metadata_server_default_matches_live_schema` (the same test class that caught a Phase 4 corrections-pass bug in `rules.default_canon_status_id()`).
3. **A `Column(...)` comment was declared in the migration but not mirrored in `tables.py`.** `world.area_connections.is_one_way` had its `COMMENT ON COLUMN` in the migration but the corresponding `tables.py` `Column()` call had no `comment=` argument, caught immediately by `alembic check` before any test ran.
4. **A flaky test, found by re-running the full suite rather than trusting one green run.** `test_a_valid_transition_between_adjacent_non_overlapping_locations_is_accepted` (added in this exit-review pass) ordered its result rows with `ORDER BY arrived_at_world_time_id` — a `UUID` primary key with no relationship to chronological order (`gen_random_uuid()` output is random, not sequential), so the assertion's row order depended on UUID-generation luck rather than the actual sequence of events. Passed on first write, failed on a later full-suite run once the random UUIDs happened to sort the other way. Fixed by ordering on `location_period` instead, which sorts by the range's actual lower bound. A reminder of exactly why the exit review's "do not rely only on the existing green CI run" instruction matters — this bug would have shipped invisibly in a single "run once and move on" pass.

None of these were schema-design defects — three were test/tooling-parity bugs caught before they could hide a real one (the same class of finding every prior phase's verification pass recorded), and one was a test itself asserting on a non-deterministic ordering.

## First exit review corrections (2026-08-03)

A review of the branch before merge found seven blocking gaps across integrity, mutation safety, and completeness that the original exit criteria above didn't happen to exercise. Closed by five forward-only revisions (043–047); revisions 038–042 were not touched.

1. **Character-location temporal integrity** — `campaign.character_location_history` had same-world checks but not the rest of the ADR 0010 interval contract `campaign.party_memberships` already implements: no overlap prevention beyond "one open row," no ordering check, and endpoints that discarded a client-supplied range only by accident (there wasn't one to discard). **Fix:** revision 043 reused the party-memberships pattern exactly — required `arrived_at_world_time_id`, a trigger-derived `location_period INT8RANGE`, `EXCLUDE USING gist (timeline_id, character_id, location_period WITH &&)`, and endpoint `ON DELETE RESTRICT` instead of `SET NULL`. **Tests:** `test_character_location_temporal_integrity.py`, 26 cases covering every scenario the review listed (closed history, adjacent transition, open current location, wrong-world arrival/departure, equal/reversed endpoints, overlapping closed/open/mixed periods, update-introduced overlap, and that `core.world_times`/`core.entities`/`campaign.timelines` immutability (revision 030) already prevents a referenced scope row from being mutated out from under existing history).
2. **Dungeon structural mutation safety** — revision 039's rules validated only at insert time (or, for dungeon areas, only from the `dungeon_areas` side). **Fix:** revision 044 made connection endpoints and feature/hazard/interactable area assignments immutable via the existing `core.enforce_immutable_columns()` (no new mechanism), and added a cycle-of-any-length guard plus a dungeon-parent revalidation trigger to `world.locations` for the columns that must legitimately stay mutable. **Tests:** `test_dungeon_structural_mutation_safety.py`, 14 cases (immutable endpoints individually and both-together-cross-world, immutable area assignments including cross-world, non-identity columns staying mutable, reparenting a dungeon area to a non-dungeon/clearing its parent both rejected, valid reparenting accepted, two-node and three-node cycles rejected, valid non-cyclic reparenting accepted, direct self-parenting via UPDATE rejected).
3. **Knowledge event timestamps** — `learned_at_world_time_id`/`discovered_at_world_time_id` were unchecked. **Fix:** revision 045 extended (`CREATE OR REPLACE`) the two existing trigger functions rather than adding new ones. **Tests:** `test_knowledge_event_timestamp_validation.py`, 9 cases (accept/reject/update for both columns, immutability of a referenced world time proven in this context specifically, and a regression check that the malformed-recipient early return still lets the CHECK constraint fire).
4. **Dungeon-state `updated_at`** — declared but never maintained on all five revision-040 tables. **Fix:** revision 046 attaches `core.set_updated_at()`, same construction and naming as everywhere else. **Tests:** `test_dungeon_state_updated_at.py`, 5 cases (one per table), using the deliberately-wrong-value-gets-overridden pattern from `test_core_lookups_and_security.py` rather than a sleep-and-compare (which would pass against a broken trigger too, since `now()` is transaction-start time).
5. **Completeness — `realm`** — DOMAIN_MODEL.md §9.1 names it, revision 038 omitted it. **Fix:** revision 047 registers it the same way the other six no-subtype-table location kinds are registered. **Test:** `test_locations.py::test_a_realm_location_can_be_created`.
6. **Completeness — conditional routes** — named in this phase's own deliverable list, not built. **Fix:** revision 047 adds `is_conditional`/`condition_description` to `world.area_connections` — the descriptive half only. Evaluation is explicitly deferred, not silently dropped: PLAN.md's Phase 6 first-time obligations now name the missing dependency (interaction/check resolution, and quest state for quest-gated conditions) and what Phase 5 does/doesn't support. **Tests:** `test_dungeon_structures.py::test_a_connection_can_be_marked_conditional_with_a_description`, `::test_a_connection_defaults_to_unconditional`.
7. **Completeness — knowledge Phase 5/7 boundary** — documentation only; nothing to fix in schema. DATABASE_MODEL.md §15 now states explicitly that temporal validity of knowledge items and real discovery source/provenance are NOT built, distinct from the three tables (`knowledge_items`, `entity_knowledge`, `party_discoveries`) Phase 5 did pull forward. (Correction from the second exit review below: this entry originally assigned both gaps to "Phase 7." Temporal validity genuinely is Phase 7's — but real discovery provenance depends on `interaction.interactions`/`narrative.events`, both Phase 6 deliverables, making it Phase 6's obligation instead; PLAN.md and DATABASE_MODEL.md §15 now say so unambiguously.)
8. **Party-navigation scenario** — the original exit-criterion evidence was a bare character moving between two dungeon areas, with no campaign, party, or party membership. **Fix:** no schema change; `tests/scenario/test_dungeon_navigation.py` builds the full chain (world → timeline → campaign → party → character → membership → three-room dungeon → connections) and moves the character through it, checking history, current location, resulting dungeon state, and one case the database actually forbids (a party member occupying two rooms at once).

None of the eight findings were caused by a wrong architectural decision — all were places where a rule was enforced on the happy path (insert) but not revalidated on mutation, or where a documented requirement was quietly dropped instead of built or deferred explicitly. The same class of gap Phase 4's corrections and closeout passes found and fixed.

### Verification commands and results

Run against the deployed AWS `dev` RDS instance (per [§23.0](PLAN.md#230-aws-verification-policy)), matching the commands `.github/workflows/ci.yml` runs:

| Command | Result |
|---|---|
| `alembic -c database/alembic.ini upgrade head` (from `042_close_phase4_location_refs`) | Applied 043→047 cleanly |
| `alembic -c database/alembic.ini downgrade 042_close_phase4_location_refs` then `upgrade head` | Round trip clean (re-run three times total across this pass, after two in-flight fixes to revision 043 — an index partiality fix and an `ON DELETE` action fix, both caught by re-reading the migration against ADR 0010 before considering it final) |
| `alembic -c database/alembic.ini check` | `No new upgrade operations detected.` |
| `ruff format --check .` | `99 files already formatted` |
| `ruff check .` | `All checks passed!` |
| `mypy src` | `Success: no issues found in 5 source files` |
| `pytest -q` (unit + database + scenario, the same bare invocation CI uses) | `1040 passed` (13 unit, 1026 database, 1 scenario) — a fresh ephemeral database created and migrated from `base` to `head` by `tests/conftest.py`, so this run is also independent proof the full 47-revision chain applies cleanly from empty |

Not re-run in this pass, and explicitly flagged rather than assumed: a full downgrade to `base` against `dev` (destructive on shared infrastructure — the bounded round trip above plus CI's own from-`base` ephemeral-database run are the accepted substitute, same as the original Phase 5 build). This pass's own fresh CI run ([`30785396702`](https://github.com/NemesisGhost/dnd_ai/actions/runs/30785396702)) completed successfully before the branch moved on to the second exit review below.

## Second exit review corrections (2026-08-03)

A second review of the branch before merge, after the first corrections pass above had already gone green on CI, found four further gaps a purely sequential, single-transaction test suite could not have exercised — plus documentation drift including the Phase 6/7 provenance ambiguity corrected in finding 7 of the pass above. Closed by four more forward-only revisions (048–051); revisions 038–047 were not touched.

1. **Parent-side entity-type-change protection** — `core.enforce_entity_subtype()` (revision 004) validates only from the subtype side: `UPDATE core.entities SET entity_type_id = ...` touches no subtype table, so nothing stopped retyping a dungeon (or any other subtyped entity) away from its type while its subtype row — and, for dungeons, its `world.dungeon_areas` children — remained. **Fix:** revision 048 adds `core.entity_types.required_subtype_pk_column` (explicit, paired with `required_subtype_table`, not derived by string manipulation) and `core.enforce_entity_type_change()`, a generic `BEFORE UPDATE OF entity_type_id` trigger on `core.entities` walking both the OLD and NEW type's ancestry and rejecting a change that would strand an existing subtype row — protects every registered subtype (character/npc/player_character, location/settlement/building/dungeon/dungeon_area, knowledge_item) through one mechanism. A second, dungeon-specific trigger (`world.enforce_dungeon_type_change_preserves_areas()`) closes the deeper case: `world.dungeons` is deletable (conventions §7.5), and once deleted the generic trigger has nothing left to check, but `world.dungeon_areas` children may still depend on the parent staying dungeon-typed. A review of every other Phase 4/5 subtype relationship found no equivalent "child requires parent to remain a specific type" rule, so no further dedicated trigger was needed. **Tests:** `test_entity_type_change_protection.py`, 9 cases (dungeon → region, dungeon → building, dungeon_area → region, a same-type no-op update, an ordinary rename, a bare-location correction before any subtype row exists, the marker-already-deleted-but-areas-remain case rejected, the marker-deleted-and-no-areas case accepted, and an NPC → bare-character rejection proving the Phase 4 coverage).
2. **Containment-cycle prevention was not concurrency-safe** — revision 044's ancestry check read the hierarchy with no lock, so two concurrent transactions (A placed under B; B placed under A, both starting from the same acyclic snapshot) could each observe no cycle and both commit — write skew, since the two writes touch disjoint rows and nothing else serializes them. **Partial fix:** `CREATE OR REPLACE` on revision 044's `world.enforce_location_no_cycle()` (the function, not the trigger attachment, which revision 044 still owns) now takes a per-world `pg_advisory_xact_lock` before reading the ancestry, keyed on both the mover's and the target parent's world and acquired in sorted order so two transactions naming the same pair of worlds can never deadlock. **Test:** `test_dungeon_structural_mutation_safety.py::test_a_concurrent_containment_swap_is_serialized_and_rejected` — two genuinely independent connections (`postgres_engine`, not two statements on one connection), transaction 1 moves A under B and stays open, transaction 2 attempts B under A under a 2-second `lock_timeout` and is proven to block (not merely fail immediately), transaction 1 then commits, and the identical swap retried afterward is rejected outright by the cycle check. This closes the tested write skew, but the added depth cutoff silently truncates at 10,000 rather than detecting repeated nodes or raising on cutoff; arbitrary-depth and corrupt-hierarchy safety remain open in [PHASE5_REMAINING_ISSUES.md item 2](PHASE5_REMAINING_ISSUES.md#2-make-containment-cycle-validation-complete-and-corruption-safe).
3. **`location_period` was not structurally `NOT NULL`** — revision 043 gave `campaign.character_location_history.location_period` a trigger that always derives it for new writes, but never declared the column itself `NOT NULL`, unlike `campaign.party_memberships.effective_period`. **Partial fix:** revision 050 asserts zero existing NULLs, then runs `ALTER COLUMN location_period SET NOT NULL`, and corrects both the column and table `COMMENT`s. It does not derive ranges for rows that existed when revision 043 added the nullable column, so a populated upgrade can stop before reaching head; [PHASE5_REMAINING_ISSUES.md item 3](PHASE5_REMAINING_ISSUES.md#3-repair-the-populated-revision-042043-upgrade-path) owns that gap. **Tests:** `test_character_location_temporal_integrity.py`, 4 new final-schema cases (catalog introspection confirms `is_nullable = 'NO'`; insert-then-close-out derivation still produces correct ranges under the tightened column; an explicit client-supplied `NULL` is overwritten by the trigger rather than surviving; an explicit client-supplied decoy range that looks non-overlapping is overwritten too, so the exclusion constraint still catches the real, derived overlap). None seeds a row before revision 043 and upgrades it through revision 050.
4. **`updated_at` tests only ever set `updated_at` directly** — proving the trigger overwrites a supplied value, never that it fires from an ordinary state-changing write. **Fix:** no schema change; five new tests extend (not replace) `test_dungeon_state_updated_at.py`, one per dungeon-state table, each inserting a row, capturing `updated_at`, mutating a real state column (`is_searched`, a status lookup FK, `is_destroyed`, ...) in a genuinely separate committed transaction, and asserting `updated_at` advanced. Genuinely separate transactions were required, not a sleep: `core.set_updated_at()` uses `now()`, which is *transaction start* time in PostgreSQL, so two statements inside one transaction (the `db_connection` fixture the original five tests use) would show no advance even with a correctly firing trigger — the fix reuses `postgres_engine`'s real, separately-committed transactions, the same mechanism the concurrency test above needs and for the identical underlying reason.
5. **Cross-world mutation coverage was incomplete for features and interactables** — the hazard immutability test already covered a cross-world reassignment attempt; the feature and interactable tests only exercised same-world reassignment. **Fix:** no schema change; added `test_a_feature_cannot_be_reassigned_to_an_area_in_another_world` and `test_an_interactable_cannot_be_reassigned_to_an_area_in_another_world`, matching the existing hazard test's shape, and all three (hazard included) now additionally assert the rejected row's `dungeon_area_id` and `is_hidden` are unchanged after the failed UPDATE — proving the rejection doesn't leave the original record corrupted, not just that it raised.
6. **Conditional-route columns permitted contradictory states** — `is_conditional = true` with no description, and `is_conditional = false` with one, were both silently allowed. No design document establishes a deliberate use for either. **Partial fix:** revision 051 adds `ck_area_connections_conditional_description_paired`: a conditional route requires a non-null description containing something after ordinary spaces are trimmed; an unconditional route must have `NULL`. **Tests:** `test_dungeon_structures.py`, 5 new cases (conditional without a description rejected, conditional with ordinary spaces rejected, unconditional with a description rejected, the same rejection on UPDATE not just INSERT, and the valid way to make a route unconditional — both columns changed together in one UPDATE — accepted). Tab/newline/carriage-return-only values are not trimmed and remain open in [PHASE5_REMAINING_ISSUES.md item 4](PHASE5_REMAINING_ISSUES.md#4-treat-all-whitespace-only-conditional-descriptions-as-blank).
7. **Documentation drift, including the Phase 6/7 provenance ambiguity** — DATABASE_MODEL.md §15 said real discovery provenance was "still Phase 6/7 work" in the same sentence that had just attributed it to Phase 6; PLAN.md's Phase 7 section separately listed that same provenance fix as something "Phase 5 explicitly left for this phase," directly contradicting Phase 6's own first-time-obligations entry for it. **Fix:** DATABASE_MODEL.md §15 and PLAN.md's Phase 7 section both now say Phase 6 unambiguously (`interaction.interactions`/`narrative.events` are Phase 6 deliverables; the replacement has no dependency on Phase 7's quest or knowledge-versioning work). The one place "Phase 6/7" legitimately remains (DATABASE_MODEL.md §9.2 and PLAN.md's Phase 6 obligations, on conditional-route *evaluation*) already distinguishes the two deliverables explicitly — general interaction/check resolution (Phase 6) versus quest-gated conditions specifically (Phase 7) — so it was left as-is rather than flattened. Also corrected: this file's own stale claims (no corrections CI run yet; test/revision counts; the `character_location_history` table comment now describes the derived range and exclusion constraint rather than the superseded index design — carried through from finding 3's migration into this file too), plus a repo-wide grep for other stale phase-status statements, test totals, and revision heads.
8. **PR #5's description** — updated after this pass's own CI run went green: migration range extended to `048`–`051`, test count corrected from 971/1040 to the final 1066, this pass's four integrity corrections summarized, and every explicit later-phase deferral preserved from the first pass's description rather than dropped.

None of these four schema-level findings were caused by a wrong architectural decision — three are the same class of gap the first pass already found (a rule enforced on the easy path but not revalidated elsewhere: insert-only in the first pass's finding 2, single-writer-only here in findings 2–3), and one (finding 6) is the same "a documented requirement's edge cases were left implicit instead of constrained" shape as the first pass's completeness findings.

### Verification commands and results

Run against the deployed AWS `dev` RDS instance (per [§23.0](PLAN.md#230-aws-verification-policy)), matching the commands `.github/workflows/ci.yml` runs. Per the second exit review's explicit instruction, the previously-green CI run (`30785396702`, for revisions 038–047) was not relied on for this pass — it predates revisions 048–051 and their tests.

| Command | Result |
|---|---|
| `alembic -c database/alembic.ini upgrade head` (from `047_realm_conditional_routes`) | Applied 048→051 cleanly |
| `alembic -c database/alembic.ini downgrade 047_realm_conditional_routes` then `upgrade head` | Round trip clean (re-run twice total: once for the initial four revisions, once more after adding the dungeon-specific trigger to revision 048) |
| `alembic -c database/alembic.ini check` | `No new upgrade operations detected.` (re-confirmed after `ruff format` reformatted five files, in case any reformatting affected `tables.py`) |
| `ruff format .` | 5 files reformatted (line-wrapping only, in the new migrations/tests from this pass), 99 unchanged; re-ran `alembic check`/`mypy`/`pytest` afterward to confirm no regressions |
| `ruff check .` | `All checks passed!` |
| `mypy src` | `Success: no issues found in 5 source files` |
| `pytest -q` (unit + database + scenario, the same bare invocation CI uses) | `1066 passed` (13 unit, 1052 database, 1 scenario) — run twice in direct succession (once before the `ruff format` pass, once after) to confirm stability, the same discipline that caught the first pass's flaky test — a fresh ephemeral database created and migrated from `base` to `head` by `tests/conftest.py` each time, so each run is also independent proof the full 51-revision chain applies cleanly from empty |

Not re-run locally in this pass, for the same reason as the first: a full downgrade to `base` against `dev` (destructive on shared infrastructure). GitHub Actions run [`30801159031`](https://github.com/NemesisGhost/dnd_ai/actions/runs/30801159031) on commit `3c8b2c6` ran it instead ("Test downgrade (full round trip to base)") and passed, alongside every other required step — `Lint and Type Check` and `Migrations and Tests (AWS dev)` both concluded `success`, including seed idempotency, schema comparison, and the full test suite.

## Third exit review corrections (2026-08-03)

A post-merge review — the first review of this phase to run against `main` rather than an open PR — found five gaps the two pre-merge passes' own sequential, single-transaction, always-fresh-database test suite could not have exercised: two "partial fix" caveats the second pass's own findings 2 and 3 already flagged as open (concurrency for the generic subtype/dungeon-area relationship, and depth-bound correctness for containment cycles), one gap the second pass's finding 3 introduced without realizing it (a `NOT NULL` guard with nothing upstream of it to guarantee the assertion holds on a populated database), one incomplete fix (`trim(both ' ' ...)` only strips ordinary spaces), and documentation/status drift. Three forward revisions (053–055) plus one narrowly scoped migration-history exception (revision 052 spliced before 050, requiring revision 050's `down_revision` to change) addressed those five findings. The fourth review below found revision 053's locking fix incomplete on one child-location path, so the register is reopened rather than a closed historical record.

1. **Entity-type-change concurrency (PHASE5_REMAINING_ISSUES.md item 1)** — revision 048's parent-side checks and revision 004/039/044's subtype-side/dungeon-area-side checks each read the other's data with no shared lock, so two concurrent transactions could validate against snapshots that predate each other's write and both commit. **Partial fix:** revision 053 adds per-entity `pg_advisory_xact_lock` protection to five functions. This correctly coordinates generic subtype insertion with retyping the same entity and coordinates dungeon-area validation with retyping the proposed parent dungeon. It does not coordinate inserting the dungeon-area subtype with changing the child location's own `parent_location_id`; the insert locks the proposed parent while the location-update defender can return before it sees the uncommitted subtype. **Tests:** `test_entity_type_change_protection.py` adds three two-connection cases, but each proves blocking by triggering `lock_timeout`, rolling back the waiter, and performing a new sequential retry. They do not prove the original waiting statement resumes and revalidates. The reopened register owns both the remaining write path and the stronger test requirement.
2. **Containment-cycle detection was incomplete (PHASE5_REMAINING_ISSUES.md item 2)** — revision 049's `WITH RECURSIVE ... UNION ALL` walk with a `depth < 10000` cutoff could loop forever around a pre-existing corrupt cycle not containing the row being updated, silently truncating at the cutoff and reporting "not found" as if that were proof of acyclicity; the same silent truncation could also let a genuine ancestor beyond the cutoff go unchecked. **Fix:** revision 054 adopts PostgreSQL's native `CYCLE location_id SET is_cycle USING path` clause for real repeated-node detection — a corrupt cycle is caught regardless of whether the target is part of it — and keeps the depth cutoff only as a resource bound, now raising a clear error when it is reached instead of treating an incomplete walk as an acyclic one. **Tests:** `test_dungeon_structural_mutation_safety.py`, 2 new cases — a pre-existing 3-node cycle constructed by bypassing the trigger (there is no other way to create one), with an unrelated "bystander" location's reparenting under the corrupt subgraph correctly rejected even though the bystander is not part of the cycle; and a bulk-constructed 10,001-node non-cyclic chain proving the depth bound now raises rather than silently accepting.
3. **The populated revision-042→043 upgrade path was never actually repaired (PHASE5_REMAINING_ISSUES.md item 3)** — revision 050's "backfill" was a NULL-count assertion, not a backfill; a database with real revision-042 history rows predating revision 043's derivation trigger would carry a NULL `location_period` into revision 050's guard and fail there, with no supported way to proceed, since no revision after 051 could reach back and fix a database that never gets past 050. **Fix:** revision 052, spliced into history between 049 and 050 (050's `down_revision` repointed here), re-fires revision 043's own derivation trigger via a no-op `UPDATE` on every row with a NULL `location_period`, reusing its validation and derivation logic rather than duplicating it — the table's own exclusion constraint then catches any genuine overlap between legacy rows exactly as it would for any other write. This is a narrowly-scoped, explicitly recorded exception to the forward-only migration policy (DATABASE_CONVENTIONS.md §37): justified because revision 050 has never run against a populated database in any environment this project has deployed, and its own DDL is completely unchanged — only what runs immediately before it changed. **Tests:** `test_phase5_populated_upgrade.py` (new file), 3 cases, each provisioning its own throwaway database and pausing mid-upgrade at revision 042: a positive fixture (one valid closed row, one valid open row) upgraded through head and asserted against the exact expected derived ranges; a negative fixture (two genuinely overlapping legacy rows) proving the upgrade aborts clearly on `ex_character_location_history_no_overlap` rather than silently succeeding or corrupting data; and a second negative fixture (a row with no arrival endpoint at all) proving the "non-derivable" case fails at revision 043 itself — on that column's own `NOT NULL` constraint — and never reaches revision 052 or 050, substantiating rather than merely asserting the docstring's claim that this case is unreachable in practice. A fresh, empty database's `alembic upgrade head` was also run directly to confirm a true from-scratch install correctly traverses the revision-052 splice point (`dev`, already past that point in the chain before this splice existed, correctly does not need to re-run it — the same standard Alembic behavior for inserting a revision retroactively into already-passed history).
4. **Conditional-route blank detection only handled ordinary spaces (PHASE5_REMAINING_ISSUES.md item 4)** — `trim(both ' ' from condition_description)` strips only the literal space character, so a tab-only, newline-only, or carriage-return-only description satisfied the "nonblank" clause. **Fix:** revision 055 replaces the trim-and-measure approach with `condition_description ~ '\S'` — a direct test for "contains at least one non-whitespace character" that does not need to enumerate every whitespace character to reject strings made only of them. The CHECK remains migration-owned rather than duplicated in SQLAlchemy: `src/dnd_ai/persistence/tables.py` explicitly excludes all CHECK constraints, triggers, and default privileges from metadata, so live migration/integration tests are the authoritative parity mechanism for this class of rule. **Tests:** `test_dungeon_structures.py`, the existing blank-description test parametrized across space/tab/newline/carriage-return/mixed-whitespace/empty-string (all rejected), plus a new positive case confirming whitespace-padded real text is still accepted (the rule is "contains real content," not "contains no whitespace at all").
5. **Documentation and status drift** — `PHASE5_VERIFICATION.md` said "Nine revisions" while its own table listed all 14 through 051; the merged documentation described Phase 5 as complete and Phase 6 as current despite the open register. **Third-pass fix:** revision/table/column counts were corrected throughout (eighteen revisions as of this pass), and status documents were advanced after the pass. **Fourth-review correction:** those status claims are reopened because the child-location race remains; the active documents now distinguish successful PR #6/CI evidence from Phase 5 closure.

None of the five findings changed the architectural direction. The fourth review shows that finding 1 was only partially closed: the parent-dungeon lock key did not protect mutation of the child location row that determines which parent is validated.

### Verification commands and results

Run against the deployed AWS `dev` RDS instance (per [§23.0](PLAN.md#230-aws-verification-policy)), matching the commands `.github/workflows/ci.yml` runs. Per this pass's own review instruction, no prior CI run (all of which predate revisions 052–055 and their tests) was relied on as evidence for this pass.

| Command | Result |
|---|---|
| `alembic -c database/alembic.ini upgrade head` (from `051_conditional_route_semantics`) | Applied 053→055 cleanly (052 is spliced before 050, both already past on `dev` — see finding 3 above) |
| `alembic -c database/alembic.ini downgrade 047_realm_conditional_routes` then `upgrade head` | Round trip clean, exercising the full 052 splice point in both directions (048→049→**052**→050→051→053→054→055 on the way up; the reverse on the way down) |
| `alembic upgrade head` against a fresh, empty database (not the shared `dev` instance) | Clean run through all 18 revisions from `base`, confirming the splice point is correctly traversed on a true fresh install |
| `alembic -c database/alembic.ini check` | `No new upgrade operations detected.` (re-confirmed after `ruff format` reformatted three files) |
| `ruff format .` | 3 files reformatted (line-wrapping only, in the new migrations/tests from this pass), 106 unchanged; re-ran `alembic check`/`mypy`/`pytest` afterward to confirm no regressions |
| `ruff check .` | `All checks passed!` |
| `mypy src` | `Success: no issues found in 5 source files` |
| `pytest -q` (unit + database + scenario, the same bare invocation CI uses) | `1080 passed` (13 unit, 1066 database, 1 scenario) — run multiple times across this pass (before and after the `ruff format` pass, and again after adding the missing-arrival-endpoint fixture) to confirm stability, the same discipline every prior pass in this phase has applied |

Not re-run locally in this pass, for the same reason as every prior pass: a full downgrade to `base` against `dev` (destructive on shared infrastructure — the bounded round trip above, the direct fresh-database `upgrade head` run, and CI's own from-`base` ephemeral-database run are the accepted substitute).

The push-triggered verification for the merged third pass is GitHub Actions run
[`30835071145`](https://github.com/NemesisGhost/dnd_ai/actions/runs/30835071145): both jobs passed, including the full migration round trip, seed idempotency, schema comparison, all 1,080 tests, database cleanup, and security-group cleanup. PR #6's unchecked historical "CI green" checkbox does not change that evidence, but the run does not cover the fourth-review race.

## Fourth exit review corrections (2026-08-03)

Review of merged PR #6 at `7ae606c` confirmed revisions 052, 054, and 055 and
most of revision 053 behaved as documented. It found one remaining write-skew
case: inserting `world.dungeon_areas` for child location L could race with a
direct change to L's `parent_location_id`, because the two paths did not share
a child-location lock before reading subtype/parent state. It also found that
the three revision-053 concurrency tests timed out and rolled back the waiter,
then used a new transaction for revalidation instead of proving the original
statement resumes on a fresh snapshot. One forward-only revision (056) plus a
rewrite of the affected tests closed both findings.

1. **Serialize dungeon-area creation with child-location parent changes (PHASE5_REMAINING_ISSUES.md, "Current schema blocker" item 1)** — `world.enforce_dungeon_area_parent_dungeon()` (INSERT/UPDATE on `world.dungeon_areas`) read the child location's `parent_location_id`, then locked only the proposed parent dungeon (revision 053); it never locked the child. `world.enforce_dungeon_area_parent_dungeon_on_update()` (UPDATE OF `parent_location_id` on `world.locations`) checked whether the child already had a `world.dungeon_areas` row *before* taking any lock, so a still-uncommitted concurrent insert was invisible to it and it returned without ever contending for a shared resource. Either transaction could therefore validate against a snapshot that predated the other's write, and both could commit — inserting a dungeon-area row for a child whose parent had just been cleared or retyped away, or vice versa. **Fix:** revision 056 adds a `pg_advisory_xact_lock` keyed on the child location's own id, `'world.locations.dungeon_area_parent:' || <location_id>::text` — a namespace distinct from revision 053's `'core.entities.subtype:'` namespace, so the two never collide even for the same location id — as the *first* statement in both functions, with each function's existing read moved to after the lock is acquired. Both functions that also need revision 053's parent-dungeon lock acquire the new child lock strictly before it, giving every transaction that can hold both locks a single consistent acquisition order (child-namespace before parent-namespace), which rules out a deadlock cycle between the two functions by the standard resource-ordering argument. A review of every other Phase 4/5 subtype relationship (`world.settlements`, `world.buildings`, `character.npcs`/`.player_characters`, `knowledge.knowledge_items`) found none couples subtype validity to a structural parent's type the way `world.dungeon_areas` does, so `world.dungeon_areas` is the only relationship this fix needed to cover — recorded per the review's explicit request even though no further function required changes. Clearing a dungeon area's parent or moving it beneath a non-dungeon is still rejected; reparenting to another dungeon and ordinary reparenting of non-dungeon-area locations are still permitted — this revision only adds locking around existing reads, it does not change any decision. **Tests:** two new two-connection tests in `test_entity_type_change_protection.py`, one per starting order — `test_a_concurrent_dungeon_area_insert_and_child_parent_clear_is_serialized` (insert commits first; the concurrently blocked parent-clearing update resumes and is rejected with "cannot be cleared") and `test_a_concurrent_child_parent_clear_and_dungeon_area_insert_is_serialized` (the parent-clearing update commits first, while the location is not yet a dungeon area; the concurrently blocked insert resumes and is rejected with "no parent_location_id"). The existing sequential positive/negative cases in `test_dungeon_structural_mutation_safety.py` (clearing rejected, non-dungeon parent rejected, reparent to another dungeon accepted, non-dungeon-area locations freely reparented) were re-run unchanged and continue to pass, confirming the added locking changed no decision.
2. **Prove the original waiting statement resumes and revalidates (PHASE5_REMAINING_ISSUES.md, "Current verification obligations")** — the three revision-053 concurrency tests set a short `lock_timeout` on the second statement, let it fail with a timeout error, rolled it back, and retried in a brand-new third connection — solid evidence that the lock attaches, but not that the *original* waiting statement itself ever resumes on the newly committed snapshot rather than simply being abandoned. **Fix:** no schema change. All three existing tests, plus the two new child-lock tests above, now use the pattern already established in `test_phase4_remaining_issues.py::_assert_blocked_delete_resumes_and_is_rejected` (a real second connection running on a background thread, held genuinely alive rather than given a `lock_timeout`), generalized here to `pg_advisory_xact_lock` waits via two new file-local helpers in `test_entity_type_change_protection.py`: `_run_blocked_statement` starts the second statement on its own thread inside an explicit transaction and records its eventual commit/failure outcome; `_wait_until_genuinely_waiting_on_a_lock` polls `pg_stat_activity.wait_event_type` for that thread's backend pid until it reports `'Lock'`, proving the thread is truly blocked rather than merely slow, before the first transaction commits. The first transaction then commits, the background thread is joined with a bounded 10-second timeout (so a failed assertion cannot hang CI or leak a transaction), and the test asserts on the thread's actual recorded outcome — in every one of these five cases, the resumed statement is rejected with the same message the equivalent third-connection retry previously asserted, now proved against the original waiting statement itself. **Tests:** `test_entity_type_change_protection.py`'s five concurrency tests (three rewritten, two new) all pass locally and were run multiple times to confirm the polling/threading logic is not itself flaky.

None of the two findings changed the architectural direction; both are the same "a rule was enforced from one side of a relationship but not coordinated with the other" and "blocking was proved but resumption wasn't" shapes the third pass's own findings 1 and 1's test gap already exhibited, just on the one path and test standard that pass's revision 053 did not reach.

### Verification commands and results

Run against the deployed AWS `dev` RDS instance (per [§23.0](PLAN.md#230-aws-verification-policy)), matching the commands `.github/workflows/ci.yml` runs. Per every prior pass's own instruction, no earlier CI run (all of which predate revision 056 and its tests) was relied on as evidence for this pass.

| Command | Result |
|---|---|
| `alembic -c database/alembic.ini upgrade head` (from `055_conditional_route_whitespace`) | Applied 056 cleanly |
| `alembic -c database/alembic.ini downgrade 055_conditional_route_whitespace` then `upgrade head` | Round trip clean |
| `alembic -c database/alembic.ini check` | `No new upgrade operations detected.` |
| `ruff format .` | 1 file reformatted (line-wrapping only, in the new test from this pass), 108 unchanged |
| `ruff check .` | `All checks passed!` |
| `mypy src` | `Success: no issues found in 5 source files` |
| `pytest -q` (unit + database + scenario, the same bare invocation CI uses) | `1082 passed` (13 unit, 1068 database, 1 scenario), up from 1,080 (13/1,066/1) at the third corrections pass's exit — a fresh ephemeral database created and migrated from `base` to `head` by `tests/conftest.py`, so this run is also independent proof the full 56-revision chain applies cleanly from empty |

Not re-run locally in this pass, for the same reason as every prior pass: a full downgrade to `base` against `dev` (destructive on shared infrastructure — the bounded round trip above and CI's own from-`base` ephemeral-database run are the accepted substitute).

The push-triggered verification for this pass is recorded once its GitHub Actions run completes; see the pull request for the confirmed run URL and outcome.

## Deliberate Scoping Decisions

- **`world.area_spawn_definitions` was not built**, despite being named in PLAN.md §9.2 and (a prior revision of) DATABASE_MODEL.md §9.2. No creature-instance or stat-block model exists anywhere in this schema — `rules.creature_types` is a bare classification lookup — and Phase 9 (encounters) is the natural first consumer. Both docs corrected to record the deferral; see DATABASE_MODEL.md §26.
- **`world.settlements` and `world.buildings` are minimal**, matching Phase 4's `character.npcs` pattern: settlements carry `population` only (government/factions are Phase 8 organization concepts; economy is intentionally deferred per DOMAIN_MODEL.md §27; districts are plain child locations; control/damage state is timeline state); buildings carry free-text `building_use` (no documented vocabulary exists).
- **Six DOMAIN_MODEL.md §9.1 location kinds with no structured data** (plane, continent, nation, region, district, geographic_feature) are plain `core.entity_types` leaves under `location`, not separate CTI tables — the same pattern `character.characters` already uses for types needing no dedicated apparatus.
- **Knowledge items carry a single simplified subject reference**, not the plural "subject entities" association DOMAIN_MODEL.md §15.1 describes. Sufficient for Phase 5's one-item-one-concealed-thing use case; Phase 7 should promote to a junction table if a real requirement for multiple subjects appears.
- **`knowledge.knowledge_versions`, `.information_transfers`, `.expertise_domains`/`.character_expertise`, and `.public_knowledge` remain deferred to Phase 7** — only the three tables Phase 5's exit criteria actually needed were pulled forward.
- **No event-linked history on any of the five new timeline-state tables**, for the same reason Phase 4's three character-state tables have none: `narrative.events` does not exist until Phase 6. Each is a single mutable current row per `(timeline, target)`, enforced by its primary key.
- **`campaign.character_state` was not given a `current_location_id` column.** DATABASE_MODEL.md §17's example prose lists "current location" among tracked state, but `campaign.character_location_history`'s open row already serves that purpose (same "`NULL` end = current" convention `campaign.party_memberships` uses), so no change to the already-shipped Phase 4 table was needed.
