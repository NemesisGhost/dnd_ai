"""Domain services — invariants, allowed transitions, and cross-cutting
resolution logic that command and query services call rather than
reimplement (docs/architecture/SYSTEM_ARCHITECTURE.md §5.4). No HTTP or
framework types belong here.
"""
